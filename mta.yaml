_schema-version: "3.1"
ID: s4_hana_app
description: A simple CAP project.
version: 1.0.0
modules:
- name: s4_hana_app-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: s4_hana_app-auth
  - name: s4_hana_app-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    readiness-health-check-http-endpoint: /health
    readiness-health-check-type: http
  build-parameters:
    builder: npm
- name: s4_hana_app-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: s4_hana_app-db
  parameters:
    buildpack: nodejs_buildpack
- name: s4_hana_app
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: s4_hana_app-auth
  - name: s4_hana_app-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: s4_hana_app-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: s4_hana_app-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - interactionsanalytical.zip
      name: interactionsanalytical
      target-path: resources/
    - artifacts:
      - interactionapp.zip
      name: interactionapp
      target-path: resources/
    - artifacts:
      - interactionsapp.zip
      name: interactionsapp
      target-path: resources/
    - artifacts:
      - interacationsapp.zip
      name: interacationsapp
      target-path: resources/
    - artifacts:
      - analyticalfioriapp.zip
      name: analyticalfioriapp
      target-path: resources/
- name: interactionsanalytical
  type: html5
  path: app/interactions_analytical
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: interactionapp
  type: html5
  path: app/interaction_app
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: interactionsapp
  type: html5
  path: app/interactions_app
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: interacationsapp
  type: html5
  path: app/interacations_app
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: analyticalfioriapp
  type: html5
  path: app/analytical_fiori_app
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: s4_hana_app-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: s4_hana_app-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: s4_hana_app-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: s4_hana_app-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: s4_hana_app-html5-srv
    service-plan: app-host
- name: s4_hana_app-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: s4_hana_app-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
